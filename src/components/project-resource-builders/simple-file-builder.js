class SimpleFileBuilder {
  constructor(filename, extension, destination) {
    this.filename = filename;
    this.extension = extension;
    this.destination = destination;
  }

  build() {}
}

module.exports = SimpleFileBuilder;
